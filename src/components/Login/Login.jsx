import React from 'react';
import s from './Login.module.css';

const Login = (props) => {
  return (
    <div className={s.login}>
      <h2>Login</h2>
      <span>some text</span>
    </div>
  );
};

export default Login;
